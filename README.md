# pythos_basics
for practicing python basics 


git clone https://github.com/ashishravath/pythos_basics.git

git status
git diff

// Clone the repository
git add . (all files in that location)
git add <file path>
git status
git commit -m "message"
git push origin main

// intialize git for local folder
git init
git remote add origin <repository link>
git branch -M main
git add .
git status
git commit -m "message"
git push origin main